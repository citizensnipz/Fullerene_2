# Canonical Commands

Use this file as the stable command catalog for humans and agents.

## Entry format
- Purpose: `<what this command verifies/does>`
- Command: `<exact command>`
- Expected result: `<success signal>`
- Notes: `<side effects, prerequisites>`

## Current status
Project command set is `UNVERIFIED`.

## Suggested sections
- Setup/install
- Run locally
- Test
- Lint/static analysis
- Build
- Database/migrations (if applicable)
- Release/deploy helpers

## Rule
Only include commands that have been run successfully at least once.
